import { TokenLookupPipe } from './token-lookup.pipe';

describe('TokenLookupPipe', () => {
  it('create an instance', () => {
    const pipe = new TokenLookupPipe();
    expect(pipe).toBeTruthy();
  });
});
